[package]
name = "metar-taf-parser"
version = "0.4.8"
edition = "2024"
description = "METAR and TAF parsing library"
license = "MIT"

[dependencies]
thiserror = "2.0.18"
serde = { version = "1.0.228", features = ["derive"] }
